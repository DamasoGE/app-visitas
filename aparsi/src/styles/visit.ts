import { StyleSheet, View } from 'react-native';

export const stylesVisit = StyleSheet.create({
  styledCalendar: {
    borderRadius: 12,
    marginBottom: 5,
    elevation: 1,
    overflow: 'hidden',
  },
  separator: {
    height: 12,
  },
});